#cloud-config
package_update: true
package_upgrade: true

packages:
  - build-essential
  - python3-pip
  - python3-dev
  - git
  - docker.io
  - wget
  - curl
  - openssl
  - unzip

runcmd:
  - curl -fsSL https://get.docker.com -o get-docker.sh
  - sh get-docker.sh
  - useradd -m -s /bin/bash threatshield
  - usermod -aG docker threatshield
  - mkdir -p /home/threatshield/{models,keys,logs,data,scripts}
  - chown -R threatshield:threatshield /home/threatshield/
  - systemctl enable docker
  - systemctl start docker
